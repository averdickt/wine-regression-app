{
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "web": "yarn workspace web dev",  // Adjust based on your Next.js scripts
    "mobile": "yarn workspace mobile start"  // For React Native/Expo
  }
}